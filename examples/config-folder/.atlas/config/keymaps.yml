bindings:
    - keys: [v]
      action: vi
      description: vim
      groups:
          - ext=md
          - ext=json

    - keys: [t]
      action: hello
      description: toast
      groups:
          - global

    - keys: [s]
      action: set-status
      description: mark as done
      groups:
          - status=todo

    - keys: [s]
      action: set-status
      description: mark as done
      groups:
          - status=todo
