- [ ] entry fields 
- [ ] fix . folders files and .atlas folder in scan
- [ ] search entries
- [ ] plan how extract metadata from entries (markdown frontmatter)
- [ ] plan how to handle custom actions (create-task, create-project, etc.)


## Extractors 

a script that gets a filename, extracts entry metadata, and returns a dictionary of the metadata.

Then the tool save the metas in entry_metas table

proble how set the entry meta from the app then? or maybe this should be hanle by another entity
